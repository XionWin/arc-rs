[package]
name = "app"
version = "0.1.0"
edition = "2021"
build="build.rs"

[dependencies]
util = { path = "../util" }
core = { path = "../core" }
platform-sdl2 = { path = "../platform/platform-sdl2" }
